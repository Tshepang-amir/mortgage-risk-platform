# Normalise to LF in the repository. The host is Windows but every execution
# environment (container, WSL2, CI) is Linux, so mixed endings otherwise churn
# on every commit.
* text=auto eol=lf

# Binary and generated files
*.png binary
*.jpg binary
*.pdf binary
*.parquet binary
uv.lock linguist-generated=true
